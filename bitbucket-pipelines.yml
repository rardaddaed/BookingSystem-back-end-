image: docker:stable

options:
  docker: true

pipelines:
  default:
    - step:
        deployment: Test
        script:
          - ./docker-compose-dependencies.sh
          - docker login --username $DOCKER_USERNAME --password $DOCKER_PASSWORD